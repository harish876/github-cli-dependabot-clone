let minimist,help,version;_f9a‍.x([["cli",()=>cli]]);_f9a‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_f9a‍.w("./util/help",[["help",["help"],function(v){help=v}]]);_f9a‍.w("./util/version",[["version",["version"],function(v){version=v}]]);




       async function cli(argsArray) {
  const args = minimist(argsArray.slice(2));
  let cmd = args._[0] || "help";

  if (args.version || args.v) {
    cmd = "version";
  }

  if (args.help || args.h) {
    cmd = "help";
  }

  switch (cmd) {
    case "version":
      version(args);
      break;

    case "help":
      help(args);
      break;

    default:
      console.error(`"${cmd}" is not a valid command!`);
      break;
  }
}
